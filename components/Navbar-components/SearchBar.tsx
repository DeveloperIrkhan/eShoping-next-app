import { Search } from 'lucide-react'
import React from 'react'

const SearchBar = () => {
  return (
    <div>
        <Search className='hoverEffect w-5 h-5 hover:text-secondary-color hover:cursor-pointer'/>
    </div>
  )
}

export default SearchBar